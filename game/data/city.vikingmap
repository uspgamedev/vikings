function multiple(val, times)
  local result = {}
  for i = 1,times do
    result[i] = val
  end
  return result
end
local width = 32
return {
  ["joinable"] = false,
  ["music"] = "music/happyland.ogg",
  ["background"] = "background/Q_x-winter_village.png",
  ["camera_offset"] = 3,
  ["things"] = {
    {
      ["type"] = [[door]],
      ["position"] = { 30, 18 },
    },
    {
      ["type"] = [[signpost]],
      ["position"] = { 8, 18 },
    },
  },
  ["locations"] = {
    ["playerstart"] = {
      [1] = 12,
      [2] = 18,
    },
  },
  ["tileset"] = tileset:new {
    name = [[city]],
    types = {
      ["I"] = tiletype:new { floor = true, imgpath = [[data/tile/ice.png]] },
      [" "] = tiletype:new { floor = false, imgpath = nil },
    }
  },
  ["height"] = 27,
  ["width"] = width,
  ["tiles"] = {
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = ' ' }, width),
    multiple({ type = 'I' }, width),
    multiple({ type = 'I' }, width),
    multiple({ type = 'I' }, width),
    multiple({ type = 'I' }, width),
    multiple({ type = 'I' }, width),
    multiple({ type = 'I' }, width),
    multiple({ type = 'I' }, width),
    multiple({ type = 'I' }, width),
    multiple({ type = 'I' }, width),
  },
}